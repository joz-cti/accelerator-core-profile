// Load overrides (to be applied to all environments)
if (file_exists($app_root . '/' . $site_path . '/overrides.settings.php')) {
  include $app_root . '/' . $site_path . '/overrides.settings.php';
}

// Load environment specific overrides (exact method of determining environment TBC)
if (file_exists($app_root . '/' . $site_path . '/settings.local.php')) {
  include $app_root . '/' . $site_path . '/settings.local.php';
}

// Do not modify this file, instead make changes to settings.overrides.php
